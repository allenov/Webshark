1 файл SH1 
Команда awk '{print "./sh2.sh "$1 " "$2}' ./list | /bin/bash

"./sh2.sh " ---- сюда фигачаться переменные 
./list --- файл откуда считываются переменные в двух столбцах разделенные <--> пробелом
$1 --- первая переменная 
$2 --- вторая переменная
| /bin/bash --- если строк более чем 1 то заменяет ЦИКЛ и делает конвеер по 1 строке.

#Очень важный нюанс между $1 есть пробел а потом только открываются ковычки с пробелом. Это разделитель переменных.   $1 " "

1 файл SH1 
echo /certificate > rsc/$1.auto.rsc
echo ---записывает кудато 
> rsc/$1.auto.rsc  --- папка rsc должна быть в том каталоге где вы находитесь создает по названию переменной $1 файл и в него записывает строку
>> --- просто дописывает в созданный файл уже первой командой >